const projFactory = (title) => {
    const arr = [];
    const addToDo = (obj) => {
      arr.push(obj);
    }
    const getTitle = () => title;
    const getArr = () => arr;

  return { getTitle, addToDo, getArr };
};

export { projFactory }